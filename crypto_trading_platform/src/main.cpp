// NOLINTBEGIN
// NOLINTEND
#include "../hdr/helper.hpp"
#include <iostream>
#include <vector>

int main() {

    Helper app{};
    app.Init();

    /*
    for (auto& order : orders) {
        order.GetValue();
    }
    */

    return 0;
}
